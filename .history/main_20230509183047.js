console.log('I am here');

//data

arrRepos = [];
arrProjects=[];
arrPackages= [];

//function to render repo cards on the .teammate-task(repo.html) #repos-id div

//function to create a new repo card with a form on the .teammate-task(repo.html) #repos-id div

//function to pin repo cards on the .teammate-task(index.html) #overview-repos div

//function to render project cards on the .teammate-task(projects.html) #projects-id div

//function to create a new project card with a form on the .teammate-task(projects.html) #projects-id div

//function to render packages cards on the .teammate-task(packages.html) #packages-id div

//function to create a new package card with a form on the .teammate-task(packages.html) #packages-id div 
